var stickyLogo = $('.sticky-logo');

/*
	Functions are reusable pieces of code
	They're basically code stored inside varibales
*/

$(window).load(function(){
   $(".sticky-logo").sticky({ topSpacing: 0 });
});
